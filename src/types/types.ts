type MapItem = {
    type: string,
    x: number,
    y: number,
    requirement: number,
    boss: number
}

type MouseState = {
    x: number,
    y: number,
    buttons: Object
}

type LandMass = {
    top: number,
    left: number,
    bottom: number,
    right: number
}